import { TrimOnBlurDirective } from './trim-on-blur.directive';

describe('TrimOnBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimOnBlurDirective();
    expect(directive).toBeTruthy();
  });
});
